I want to change the post-login flow in my SecureQuiz platform. Currently, after a user logs in via Google, they are dropped directly into the quiz creation tool (create.html). I want to change this so that after a successful login, the user is presented with a 'Personal Account' dashboard.

Please provide the code updates needed to achieve the following:

Post-Login Routing: After login, display an 'Account Overview' screen instead of the quiz creator. (You can either repurpose the overview view in dashboard.html or create a new account.html).

User Profile Section: Display the logged-in user's basic Google data fetched from Supabase Auth (e.g., Profile Picture, Full Name, and Email).

My Quizzes Section: Below the profile data, display a grid/list of all the quizzes this user has previously created. (You can reuse the logic from the loadMyQuizzes() function currently in dashboard.js).

Call to Actions: Add prominent buttons on this personal account page to 'Create a New Quiz' (linking to the creation form) and 'View Analytics'.

Review my uploaded HTML and JS files (create.html, create.js, dashboard.html, dashboard.js) and provide the step-by-step code changes required to implement this new user flow."